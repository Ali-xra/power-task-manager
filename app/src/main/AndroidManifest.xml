<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- App permissions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- File permissions for backup/restore -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />

    <application
        android:name=".PowerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlixraPower"
        tools:targetApi="31">

        <!-- Login Activity -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AlixraPower">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.AlixraPower" />

        <!-- Alarm Activity -->
        <activity
            android:name=".ui.AlarmActivity"
            android:label="@string/morning_challenge_title"
            android:screenOrientation="portrait"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:exported="false" />

        <!-- Evening Activity -->
        <activity
            android:name=".ui.EveningActivity"
            android:label="@string/evening_activity_title"
            android:screenOrientation="portrait"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:exported="false" />

        <!-- Stats Activity -->
        <activity
            android:name=".ui.StatsActivity"
            android:label="@string/stats_activity_title"
            android:exported="false" />

        <!-- Alarms Activity -->
        <activity
            android:name=".ui.AlarmsActivity"
            android:label="@string/alarms_activity_title"
            android:exported="false" />

        <!-- Goals Activity -->
        <activity
            android:name=".ui.GoalsActivity"
            android:label="@string/goals_activity_title"
            android:exported="false" />

        <!-- Tasks Activity -->
        <activity
            android:name=".ui.TasksActivity"
            android:label="@string/tasks_activity_title"
            android:exported="false" />

        <!-- Calendar Activity -->
        <activity
            android:name=".ui.CalendarActivity"
            android:label="@string/calendar_title"
            android:exported="false" />

        <!-- Backup Activity -->
        <activity
            android:name=".ui.BackupActivity"
            android:label="@string/backup_activity_title"
            android:exported="false" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.SettingsActivity"
            android:label="@string/settings_title"
            android:exported="false" />

        <!-- Alarm Service -->
        <service
            android:name=".service.AlarmService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <!-- Evening Service -->
        <service
            android:name=".service.EveningService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <!-- Receivers -->
        <receiver android:name=".receiver.AlarmReceiver" android:exported="false" />
        <receiver android:name=".receiver.EveningReceiver" android:exported="false" />
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

    </application>

</manifest>